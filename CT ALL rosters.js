var roster = document.querySelectorAll('input[type="checkbox"]');

for (i = 0; i < roster.length; i++) {
	roster[i].checked = true;
};

element = $( "label:contains('CSV')" ); element.click();


123-181
var roster = document.querySelectorAll('input[type="checkbox"]');
for (i = 123; i < 182; i++) {
	roster[i].checked = true;
};

var roster = document.querySelectorAll('input[type="checkbox"]');
roster[141].checked = false;

element = $( "label:contains('CSV')" ); element.click();

141